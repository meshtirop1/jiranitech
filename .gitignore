*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# public/build is deliberately NOT ignored. The cPanel target has no Node runtime,
# so compiled CSS, JS and the self-hosted font files must travel with the release.
# Run "npm run build" before packaging or committing.
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# Local development database - never committed.
database/*.sqlite
database/*.sqlite-journal
